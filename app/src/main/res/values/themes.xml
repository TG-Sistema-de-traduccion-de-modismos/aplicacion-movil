<?xml version="1.0" encoding="utf-8"?>
<!-- res/values/styles.xml -->
<resources>
    <!-- Tema base claro con barras transparentes -->
    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colores principales -->
        <item name="colorPrimary">@color/red_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/red_primary_dark</item>
        <item name="colorOnSecondary">@color/white_secondary</item>
        <item name="colorTertiary">@color/gray_light</item>
        <item name="colorOnTertiary">@color/white</item>



        <!-- Superficies -->
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/text_black</item>
        <item name="colorOnSurfaceVariant">#E2373737</item>

        <item name="colorOnBackground">@color/white</item>

        <!-- Status y Navigation bar -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightNavigationBar">true</item>

        <!-- Botones -->
        <item name="materialButtonStyle">@style/AppButton</item>
        <item name="borderlessButtonStyle">@style/AppButton.Borderless</item>
    </style>

    <style name="AppButton" parent="Widget.Material3.Button">
        <item name="android:layout_height">56dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">28dp</item>
    </style>

    <style name="AppButton.Borderless" parent="Widget.Material3.Button.TextButton">
        <item name="android:layout_height">48dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/red_primary</item>
    </style>

</resources>
